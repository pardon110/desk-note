提出合理的问题，问题就解决了一半了。

urlencode问题？
markdown 服务找不到类的问题？

python线程与进程的区别


序语言的基础关键点： 作用域，函数，参数 

机器码 字节码  电平 模拟电路






js 6种 原始类型: Boolean, Null, Undefined, Number, Stirng, Symbol
值传递:

    只将值进行传递，与原变量无关，值会被复制一份，保存在内存中另一个不同的位置。

    当我们修改传递后的值时，修改的是内存中新的位置，而原变量不会被影响，还是原来的值。

    这种情况往往出现在6中原始类型中。


引用传递:

    将内存地址进行传递，相当于把原变量完整的传递过来。

    当我们修改传递后的值时，修改的是内存中原来的位置，会对原变量造成影响。

    这种情况往往出现在对象上。


关健点，传引用，传值，传地址


6.堆栈是两种数据结构.抽象数据类型。栈（stack)后进先出，定长的诸如局部变量
  堆(heap)o
  
